<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="NoticeMapper">
	<resultMap type="Notice" id="noticeResultMap">
		<id property="noticeNo" column="NOTICE_NO"/>
		<result property="userNo" column="USER_NO"/>
		<result property="noticeTitle" column="NOTICE_TITLE"/>
		<result property="noticeContent" column="NOTICE_CONTENT"/>
		<result property="nCreateDate" column="N_CREATE_DATE"/>
		<result property="nUpdateDate" column="N_UPDATE_DATE"/>
		<result property="noticeViews" column="NOTICE_VIEWS"/>
		<result property="noticeFilename" column="NOTICE_FILENAME"/>
		<result property="noticeFilepath" column="NOTICE_FILEPATH"/>
	</resultMap>

<!-- 	공지사항 리스트 -->
	<select id="selectAllNotice" resultMap="noticeResultMap">
		SELECT * FROM NOTICE_TBL
	</select>
	
<!-- 	공지사항 디테일 -->
	<select id="selectOneNotice" resultMap="noticeResultMap">
		SELECT * FROM NOTICE_TBL WHERE NOTICE_NO = #{noticeNo}
	</select>
	
	<insert id="insertNotice">
		INSERT INTO NOTICE_TBL VALUES(SEQ_NOTICE_NO.NEXTVAL, 1, #{noticeTitle}, #{noticeContent}, DEFAULT, DEFAULT, DEFAULT, NULL, NULL)
	</insert>
</mapper>